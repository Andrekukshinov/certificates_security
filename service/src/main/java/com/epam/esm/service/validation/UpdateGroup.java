package com.epam.esm.service.validation;

import javax.validation.groups.Default;

/**
 * Interface for validation objects in case of update operations
 */
public interface UpdateGroup extends Default {
}
